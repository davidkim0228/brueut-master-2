<%= form_with(model: jobpost, local: true) do |form| %>
  <% if jobpost.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(jobpost.errors.count, "error") %> prohibited this jobpost from being saved:</h2>

      <ul>
      <% jobpost.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content %>
  </div>

  <%= form.hidden_field :post_id, value: current_user.uid.to_s %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
